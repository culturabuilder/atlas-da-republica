# Dependências dos conectores e do build. Mantenha em sincronia com .github/workflows/daily.yml.
pyyaml      # todos os conectores e o build do grafo
pillow      # fotos, fotos oficiais e as imagens de compartilhamento
anthropic   # resumos em linguagem simples, quando o provedor é a API
openpyxl    # renúncias fiscais: o demonstrativo da Receita vem em xlsx
